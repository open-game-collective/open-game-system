{
  "name": "stream-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/stream-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "inputs": ["extensionManifest"],
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/stream-server",
        "main": "apps/stream-server/src/main.ts",
        "tsConfig": "apps/stream-server/tsconfig.app.json",
        "assets": ["apps/stream-server/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/stream-server/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "stream-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "stream-server:build:development"
        },
        "production": {
          "buildTarget": "stream-server:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/stream-server/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/stream-server/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
